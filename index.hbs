{{!< default}}
{{> header}}

	<section class="page-content primary" role="main">
		{{#foreach posts}}

			<article id="post-{{id}}" class="post {{post_class}}" role="article" itemscope itemtype="http://schema.org/Article">

			    {{content words = "0"}}

			    <div class="content-wrapper">
				    <h2 class="post-title">
						<a href="{{{url}}}" rel="bookmark">
				    		{{title}}
				    	</a>
				    </h2>

				    <div class="post-meta">
						Posted on
						<span datetime="{{date format="YYYY-MM-DD"}}" itemprop="datePublished">
							{{date format='MMMM Do, YYYY'}}
						</span>
						<!--{{#if author}}
							by {{author.name}}
						{{/if}}
		                {{#if tags}}
		                	<div class="post-tags">
		                		Tags:
			                    {{tags seperator=", "}}
			                </div>
		                {{/if}}-->
					</div>

				    <div class="post-content" itemprop="articleBody">

						<p>
							{{excerpt}} <span class="post-continue">&hellip;</span>
							<b class="quote-title"><br /> - {{title}}</b>
						</p>

						<a class="read-more" href="{{{url}}}">Continue Reading &rarr;</a>

					</div>

				</div>

			</article>

		{{/foreach}}

	    {{#if pagination}}
			<div class="pagination" role="pagination">
				{{{pagination}}}
			</div>
	    {{/if}}

	    {{> footer}}
	</section>